# PatchGraph Frontend

Initial browser-native frontend scaffold for PatchGraph.

Current scope:

- select a local repository directory with the File System Access API
- recursively scan the project tree in the browser
- render a basic explorer with simple repository stats

Commands:

```bash
npm install
npm run dev
npm run build
npm run lint
```
